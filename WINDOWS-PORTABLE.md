# Windows x64 便携版

便携版面向 Windows 10/11 x64。完整解压后双击 `启动翻译工作台.bat` 即可，
不需要预装 Python，也不需要管理员权限。请不要直接在 ZIP 内运行，也不要放进
`Program Files` 等只读目录；任务、术语修改和提示词修改都会保存在这个文件夹中。

基础包可以上传 SRT、获取可用的 YouTube 源语言字幕并调用用户自己选择的模型完成
翻译。首次翻译时仍需由用户在页面内输入自己的 API Key；密钥只保留在当前页面内存，
刷新或关闭页面后消失。

为控制体积并避免不透明的二进制再分发，基础包不内置 FFmpeg、Whisper、CUDA 或模型：

- 没有 FFmpeg 时，工作台会自动关闭“下载视频”；YouTube 原生 VTT 由工作台
  在本机转换为 SRT，封面保留原始图片格式，因此字幕获取与翻译仍可使用。
- 没有 Whisper 时，工作台会固定使用 YouTube 字幕；无源语言字幕的视频需要用户先
  提供 SRT，或另行安装可选 Whisper 环境。
- 如果系统已经安装 FFmpeg 并加入 PATH，重启工作台后会自动启用视频下载。

停止服务请关闭启动窗口，或双击 `停止翻译工作台.bat`。换电脑时可以复制整个已解压
文件夹；若不希望携带历史任务，复制前删除 `download` 文件夹。
