# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)
的基本格式。Demo 阶段暂不承诺语义化版本兼容性。

## [未发布]

### 文档

- 将旧私有工作区、安全门禁与数据盘点文档标为历史快照，补充公开版安全与隐私状态；README 修正 MIT、已公开仓库、公开版无私有 benchmark，以及已发布便携版的实际入口。文档变更不改翻译或审校逻辑。

## [0.2.0-beta.6] - 2026-09-23

### 文案修正

- 公开仓库简介改为说明《鸣潮》字幕翻译、三种源语言、预览审校与下载；仓库首页增加 Releases 入口。
- README、便携版说明和工作台指引统一 API Key 的会话/安全保存选择，并明确自动字幕须在必审项清零后才能作为交付版。翻译逻辑与质量门禁未改。

## [0.2.0-beta.5] - 2026-09-23

### 优化

- 完成任务的工作台顶部直接展示中文字幕前八条预览和下载入口；必审风险未清零时仍只能下载标明未审校的自动版。
- 审校默认筛选只显示尚未完成的必审风险，并修正“全部风险”筛选；自动处理与参考项仍可查看。
- 输入新 API Key 时明确选择仅本次程序会话或安全保存到 Windows Credential Manager；密钥不进入设置 JSON、任务文件或浏览器存储。

### 验证

- 独立 `.git` 隔离副本：1128 项 Python 测试通过，6 项按既有条件跳过；28 项 Node 前端测试与 JS 语法检查通过。

## [0.2.0-beta.4] - 2026-09-23

### 修复

- 窄屏工作台标题不再被角色贴图遮挡；普通提示采用中性提示样式。
- 批量重试翻译任务前检查是否已保存 API Key，并说明缺少配置时的处理步骤。

### 验证

- Windows CI 增加前端行为测试和便携包实际构建、SHA-256 校验。

## [0.2.0-beta.3] - 2026-09-10

### 新增

- 工作台主流程收敛为“链接与 API 配置 → 下载 → 直接翻译 → 结果导出”；普通配置原子持久化，API Key 使用 Windows Credential Manager，安全存储不可用时仅保留在当前会话。
- 新增 Windows CI 与 `cc` 用户目录修复脚本，fresh clone 不再依赖本机 `download/` 私有任务夹具。

- 真实任务驱动的错误与韧性修复包（2026-09-06）：新增独立错误码 `quota_error`，账户欠费（HTTP 403 + insufficient_user_quota）与订阅限额（HTTP 429 + GoUsageLimitError 等）不再误报成“Key 无效”，且自动续跑与传输退避对额度类失败一律跳过；瞬时失败改为有界自动续跑（≤3 次指数退避，可被取消/暂停打断，续跑历史落盘任务 JSON）；`/start` 与 `/retry` 端点支持代理参数透传（补上“继续失败批次”无法带代理的断链）。
- 审校队列自动分流（零 API）：外部大牌实体/误报普通词白名单与英语源冲突条目自动出队（确定性 5% 抽检回留），`review_required` 旗标与交付门控不变；风险卡新增“自动放行依据/抽检样本”徽标；成品未翻译英文句计数上任务卡。
- 质量与韧性：括号音效标注（[laughter] 类）归类 MUSIC_SFX 不再送翻译；MIXED 混合句纳入 Round 1 翻译（风旗保留）；不足 300ms 的闪帧字幕在发布前顺延或并入邻句（正式成品与人工审校版两条管线都生效）；config 快照完整性校验（损坏即早退并提示从头重跑）；完成任务重跑时 inputs 自愈回锚“过程文件/”；文件整理对 Windows 句柄瞬时冲突自动重试；只下字幕的 URL 任务完成后可补充剪映第二源并重新翻译。

- YouTube 下载改为"错误分类驱动的韧性下载"（参考开源项目 Y2A-Auto 的 `youtube_handler`）：先按关键字把 yt-dlp 失败分为 8 类（模块缺失 / Cookie 解密 / 限流 / 机器人验证 / 会话不匹配 / 格式不可用 / 合并崩溃 / 未知），再按类别行动——格式不可用自动降级到单一流（保证有音轨，画质可能下降）、会话不匹配自动摘掉 Cookie 用匿名身份重试、yt-dlp 合并崩溃自动改用本机 ffmpeg `-c copy` 合并并清理残留分片；实际子进程调用上限 3 次，取消语义与公开签名不变。错误摘要只取最后一条 `ERROR:` 并抹掉文件系统路径，避免日志刷屏与凭据/路径泄露。下载子进程显式声明本机 JS runtime（deno/node），JS challenge 解算不再依赖自动探测。常见问题新增 3 条对应的处理说明（自动降级 / 会话不匹配匿名重试 / ffmpeg 本地合并）。

### 优化

- manifest 持久化改为节流合并写入：失败与取消立即落盘，运行中与成功批次累计 8 批或超过 30 秒才合并写一次，终态强制刷新。消除每批整份 JSON 重写带来的 O(n²) 开销与并发批次串行化，2000 条 / 63 批实测写入耗时 1165.8ms → 155.9ms（−86.6%）。可通过 `MANIFEST_FLUSH_EVERY`、`MANIFEST_FLUSH_SECONDS` 调整；进程被硬杀时最多丢失 7 批落盘进度，断点续跑会重译这些批次。
- 单批 API 调用增加尝试预算（`LLM_BATCH_ATTEMPT_BUDGET`，默认 6）：传输层反复退避且语义补译仍不通过时停止继续重试，收敛最坏情况 3 次传输重试 × 3 次语义补译 = 9 次调用的放大。不关闭任何质量门禁，只停止重复掷骰子。

### 修复

- 术语库打开时不再无条件重写文件：幂等回填 `UPDATE` 增加"仅存在 NULL 值时才执行"守卫，`data/glossary.db` 在无逻辑变化时字节内容保持稳定，消除"术语库被神秘修改"类误报；旧 schema 的迁移回填行为不变。

## [0.2.0-beta.2] - 2026-08-28

### 新增

- 新增 Windows 10/11 x64 便携包构建器：内置官方 CPython 与 Deno，使用固定版本、SHA-256 下载校验和 hash-locked Python 依赖，解压后可直接双击启动。
- 新增便携版使用说明、第三方组件声明、运行时组件清单、逐文件 manifest、ZIP 校验文件和独立发布回归。

### 优化

- 运行环境没有 Whisper 时自动固定为 YouTube 字幕；没有 FFmpeg 时自动关闭视频下载，但上传 SRT 和字幕翻译仍可继续。
- YouTube 字幕改为下载原生 VTT 后由纯 Python 转为 SRT，封面保留原始图片格式；字幕与封面获取不再暗中依赖 FFmpeg。
- 便携基础包明确不捆绑 FFmpeg、Whisper、CUDA 或模型，避免把超大二进制和未处理完整的再分发义务混进首个测试包。

### 安全

- 便携包继续从 committed `HEAD` 的脱敏白名单构建，排除 API Key、Cookie、任务、报告、翻译记忆、本机路径和账号标识；依赖安装必须通过锁文件哈希校验。
- 便携模式固定调用包内 hash-locked `yt-dlp` 模块，不执行 PATH 中可能同名的外部程序；打包时额外只读扫描 SQLite 初始词库中的密钥、邮箱与个人路径模式。
- 启动器使用隔离的内置 Python 搜索路径，并在只读目录中提前停止，避免运行到中途才暴露 SQLite 或任务目录写入错误。

## [0.2.0-beta.1] - 2026-08-28

### 新增

- 发布构建器现在直接生成带 Beta 标识的 Windows 源码 ZIP 和独立 SHA-256 校验文件。
- 新增真实 YouTube 任务的 leakage-checked Benchmark 候选包生成器；候选固定为 provisional/unreviewed，审核 CSV 不预填 reviewer 或人工决定。
- 新增 2026-08-23 三条 EN/JA/KO 真实 YouTube 字幕流程、机器审计与只读指标报告，全程未运行 Whisper。
- 新增 17 个既有任务的只读指标汇总、固定三语 confirmed ID 的离线 benchmark、输入哈希与 approved TM 泄漏检查。
- 新增 benchmark 人工复核 CSV 导出/导入、默认 dry-run、原子 apply 与独立审计 ledger；自动身份不能冒充 reviewer。
- 新增官方证据 sidecar 和数据驱动 glossary adjudicator，支持备份、事务、冲突检测、幂等与 apply 后验证。
- 新增 TM/few-shot 质量审计、Round 2 成本收益、统一错误标签以及严格单变量 A/B 比较和 Critical regression veto。

### 修复

- 修复英语缩写被人名归一化污染、通用武器/卡池词被硬术语强制、英文滚动残片与标点粘连，以及不完整模型响应保留错位 ID 的风险。
- 修复日文 `[叫び声]` 纯音效漏检、仲裁层生成阻断 ID `0`、JA/KO Round 2 保留源文字，以及 resume 后耗时统计只剩恢复开销的问题。
- few-shot 相关性改为逐字幕 cue 评分，不再把不同 cue 的弱重叠合并成一个候选。
- benchmark CLI 在 Windows 强制 UTF-8 输出，韩文报告不再因 GBK 控制台编码失败。
- 官方英文拼写更新为 `Stridergate` 与 `Aleph-1`，中文目标和术语总数保持不变。

### 安全

- 新安装默认直连，不再预填维护者电脑上的本地代理；停止脚本只结束由同一解压目录启动的工作台，不再按端口强制结束无关程序。
- 所有新质量工具默认离线或 dry-run；缺少 `LLM_API_KEY` 时付费 benchmark 在任何外部请求和输出写入前停止，且不显示密钥。
- 本轮未运行 Whisper、未自动批准 TM、未升级 provisional Gold，也未把 ASR 相似度候选写入 correction 表。

## [0.2.0] - 2026-08-18

### 新增

- 新增日语、韩语到简体中文的完整流水线，与英语模式共享工作台、断点续跑和人工审校能力。
- 新增三语独立 Prompt、术语、ASR 修正、翻译记忆与 Gold/Blind 质量验收数据。
- 新增 YouTube 字幕、多源仲裁、确定性重建门禁和仅处理强风险的 Round 2 复核。
- Round 1 注入视频标题、频道、全片锁定专名、最近十二条对话与前后六条局部证据。
- 人工改译写入本机翻译记忆；只有显式批准且英文完全一致的记录才允许跨任务复用。

### 优化

- Token 硬上限内优先保持临近完整句，不再只按固定字幕条数切断快速 Reaction 残句。
- 新上下文与翻译记忆进入 manifest 签名；旧任务仍可兼容恢复，但不会静默重写已完成译文。
- Windows 安装、依赖自检、发布包构建和 README 已统一为可供受邀测试者使用的 0.2.0 版本。

### 安全

- API Key 只从会话输入或环境变量读取；上传前已从全部可达 Git 历史中清除旧硬编码密钥。
- Cookie、`.env`、任务目录和带日期的本地数据库备份不会进入发布包或 Git 提交。

## [0.1.2-demo] - 2026-07-30

### 新增

- 已知人物名与可靠人物别名强制进入人工确认，正确译名不单独触发 Round 2。
- DeepSeek V4 Flash / Pro 按官方人民币缓存未命中价显示保守费用估算。
- 工作台请求层、统一错误对象、自定义确认框与会话 API Key 输入框。
- 可复现的 GitHub 发布目录构建脚本与 SHA-256 文件清单。

### 优化

- 统一任务状态与阶段中文文案，未知后端阶段不再直接暴露英文状态。
- 任务选择不再额外重绘整个列表；SSE 同状态更新只刷新阶段与进度。
- 任务、风险与术语搜索丢弃过期响应，避免快速切换时旧数据覆盖新页面。
- 任务、风险、FAQ 与术语筛选可以从 URL 恢复，补充加载、错误和重试状态。
- 更新人物别名与 Whisper 受限目录扫描，减少人名漏审和 Windows 权限异常。

### 修复

- 修正 README 版本、不可变阶段文件名和已移除快捷键与真实代码不一致。
- 修正非 DeepSeek 模型错误显示 `$0.0000` 的费用问题。

## [0.1.1-demo] - 2026-07-29

### 修复

- YouTube 滚动字幕的 10 毫秒有效续句改为翻译前合并，不再直接删除。
- 自适应模式会识别滚动残句并请求独立 Whisper 证据；补充证据失败时可安全回退到可用的 YouTube 字幕。
- 移除 `Pulling → 卜灵` 错误强制映射，并阻止 Cover、Brand 等普通词触发模糊人名。
- 补充库洛游戏、四枚发簪、利威尔等 Reaction 常见 ASR 修正。
- 风险队列新增明显残句检测，并修正“非常”被误判为否定表达的问题。
- Round 2 默认保留 Round 1，拒绝未产生变化、无关重写、新增否定和未修完残句的候选。
- Round 2 结果 schema 升级，旧版高风险自动替换结果不会继续复用。

## [0.1.0-demo] - 2026-07-28

### 新增

- YouTube 英文字幕下载与视频清晰度选择。
- 快速模式：YouTube 字幕与自适应本地 Whisper。
- 高质量模式：剪映与 YouTube/Whisper 双英文源校对。
- Round 1 全量翻译、断点续跑和有限并发。
- Round 2 结构化风险复核与缺失 ID 局部补请求。
- 不可变阶段产物、独立风险结果和人工审校状态。
- 鸣潮术语锚定、人名识别别名和残留英文复检。
- 对已命中术语增加逐条官方译名校验与局部重试。
- 对“忘记名字”等上下文中的疑似 ASR 人名增加人工审核兜底。
- 风险差异高亮、快捷键、批量确认和未保存提醒。
- 下载器、FFmpeg 与本地 Whisper 依赖自检。
- 原创英文测试字幕 `examples/demo.en.srt`。

### 优化

- 降低弱风险误报，弱风险不再阻塞最终字幕。
- 清理音乐标记、滚动字幕、极短字幕和重复字幕。
- Round 1 自适应批次与有限并发。
- Round 2 两批并发、429 自动降级和连接复用。
- 桌面端任务管理、质量指标和最终字幕下载入口。
- 风险审核改为纯文本对照，移除视频播放器与播放快捷键。
- Round 1 与 Round 2 支持独立选择模型，并记住用户选择。
- 任务支持暂停、继续；等待上传剪映字幕时也可直接删除。
- Whisper 子进程日志会实时显示在网页技术日志中；VAD 依赖不可用时自动关闭 VAD 并继续识别。
- Windows 会自动发现本机 NVIDIA Python 依赖并配置 cuDNN/cuBLAS；Web 整段 Whisper 在 VAD 运行库故障时直接使用稳定的 GPU 无 VAD 路径。
- 统一 Web、下载器和本地 Whisper 的项目运行环境。

### 已知限制

- 当前仅支持英文到简体中文。
- 日语和韩语翻译尚未开放。
- 剪映字幕需要用户手工导出并上传。
- Whisper 首次运行需要下载模型。
- 模型中转站的可用性和速率限制不受本项目控制。
